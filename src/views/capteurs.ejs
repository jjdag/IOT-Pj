<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Capteurs - <%= Func %> </title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-r from-gray-800 via-red-700 to-yellow-600 font-sans text-gray-100">

    <header class="bg-gradient-to-r from-red-700 to-brown-800 text-white p-6 shadow-lg flex justify-between items-center">
        <div class="text-2xl font-bold uppercase tracking-wide">
            <i class="fas fa-leaf mr-2"></i> EcoLodge
        </div>
        <div class="w-1/3">
            <input type="text" placeholder="Search for a capteur..." class="w-full p-2 rounded-lg border border-gray-600 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-red-500 text-gray-900">
        </div>
    </header>

    <div class="container mx-auto px-4 py-8">
        <div class="max-w-4xl mx-auto bg-white rounded-xl shadow-xl p-6 space-y-6">

            <h2 class="text-3xl font-semibold text-red-600 text-center mb-6">Capteurs - <%= Func %> </h2>

            <div class="space-y-4">
                <% capteurs.forEach(function(capteur, index) { %>
                    <div class="bg-gray-100 p-4 rounded-lg shadow-md">
                        <h4 class="text-xl font-semibold text-yellow-500">Capteur: <%= capteur.Type %></h4>
                        <p class="text-gray-700">RefComm: <%= capteur.RefComm %></p>
                        <p class="text-gray-700">RefPiece: <%= capteur.RefPiece %></p>
                        <p class="text-gray-700">Port: <%= capteur.Port %></p>
                        <p class="text-gray-700">Insertion: <%= capteur.Insertion %></p>

                    </div>
                <% }) %>
            </div>

        </div>
    </div>

    <footer class="bg-red-700 text-white py-6 mt-8">
        <div class="text-center">
            <p>&copy; 2024 EcoLodge - All rights reserved.</p>
        </div>
    </footer>

</body>

</html>
